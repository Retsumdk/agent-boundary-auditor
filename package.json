{
  "name": "agent-boundary-auditor",
  "description": "System for monitoring and auditing agent boundary violations (PII leaks, unauthorized tool usage)",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}